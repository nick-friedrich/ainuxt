{
  "name": "with-vue-nuxt",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "sqlite:prisma": "pnpm --filter @packages/prisma-sqlite run prisma"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "turbo": "^2.5.0"
  },
  "packageManager": "pnpm@8.15.6"
}
